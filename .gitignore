# Repo-level ignores. Each project (backend/, mobile-app/, frontend/) keeps its
# own .gitignore for tooling-specific artifacts.

# OS / editor
.DS_Store
*.log
*.swp
*.iml
.idea/

# Stray build artifacts (the Flutter app lives in mobile-app/)
.dart_tool/
.flutter-plugins-dependencies
/build/
