# JAGO Frontend (Web)

Placeholder for the JAGO **web frontend**. This project is not built yet.

It will consume the same [`backend`](../backend) API the
[`mobile-app`](../mobile-app) uses (JWT auth, `/api/v1/*`), reusing the
`{data}` / `{error}` response envelope.

> Stack TBD (e.g. React/Vite or Next.js). Scaffold to be added here.
